* text=auto eol=lf
*.cmd text eol=crlf
*.bat text eol=crlf
*.ps1 text eol=crlf
# The mod writes HeadTracking.ini with CRLF and the user edits it in place;
# checking it out as LF would make the reference copy differ from the shipped one.
*.ini text eol=crlf
*.dll binary
*.asi binary
*.zip binary
*.gif binary
